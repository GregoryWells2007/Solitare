#include "normal_list.h"

// void list_create(list* new_list) {
//     new_list->count = 0;
//     new_list->items = NULL;
// }
// void list_add(list* list_item, void* data) {
//     list_item->count++;
//     if (list_item->count > list_item->items_size) {
//         list_item->items_size = list_item->items_size * GROWTH_FACTOR;
//         new_list->items = realloc(list_item, );
//     }
// }

// int list_size(list* list_item) {
    
// }