#pragma once
#include "stdlib.h"

#define GROWTH_FACTOR 2

// typedef struct list {
//     void** items;
//     int items_size;
//     size_t

//     int count;
// } list;

// void list_create(list* new_list);
// void list_add(list* list_item, void* data);

// int list_size(list* list_item);